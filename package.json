{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler pages dev public",
    "update-kysely-types": "env-cmd -f .env.local kysely-codegen --out-file ./data/schema.ts --exclude-pattern spatial_ref_sys --dialect postgres"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "env-cmd": "^10.1.0",
    "kysely-codegen": "^0.10.0",
    "pg": "^8.10.0",
    "wrangler": "^2.0.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.4.3",
    "kysely": "^0.24.2",
    "zod": "^3.21.4"
  }
}
